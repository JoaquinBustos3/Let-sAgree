export function retrieveImages(category: string, results: any[], zip: number) {

    // TMDB API
    if (category === "Movies" || category === "TV Shows") {
        // Implement image retrieval logic for Movies and TV Shows
    }
    // Yelp API
    else if (category === "Restaurants" || category === "Takeout-Delivery" || category === "Things To Do Nearby") {

        

    }
    // 
    else if (category === "Games") {
        // Implement image retrieval logic for Games
    }
    // Unsplash API
    else {

    }

}